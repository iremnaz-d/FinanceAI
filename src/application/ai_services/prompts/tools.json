[
  {
    "functionDeclarations": [
      {
        "name": "get_expenses_by_month_markdown",
        "description": "Returns the pandas dataframe in markdown format for wanted timeframe. Returns only expenses, not incomes.",
        "parameters": {
          "type": "OBJECT",
          "properties": {
            "month": {
              "type": "STRING",
              "description": "wanted month in %B format (e.g., 'March')."
            },
            "year": {
              "type": "STRING",
              "description": "wanted year in %Y format (e.g., '2026')."
            }
          },
          "required": [
            "month",
            "year"
          ]
        }
      },
      {
        "name": "get_expenses_by_month_interval_markdown",
        "description": "Returns the pandas dataframe in markdown format for wanted time interval. Returns only expenses, not incomes.",
        "parameters": {
          "type": "OBJECT",
          "properties": {
            "first_month": {
              "type": "STRING",
              "description": "wanted first month of the interval in %B format (e.g., 'March')."
            },
            "first_year": {
              "type": "STRING",
              "description": "wanted first year of the interval in %Y format (e.g., '2026')."
            },
            "second_month": {
              "type": "STRING",
              "description": "wanted second month of the interval in %B format (e.g., 'June')."
            },
            "second_year": {
              "type": "STRING",
              "description": "wanted second year of the interval in %Y format (e.g., '2026')."
            }
          },
          "required": [
            "first_month",
            "first_year",
            "second_month",
            "second_year"
          ]
        }
      },
      {
        "name": "get_expenses_markdown",
        "description": "Returns the pandas dataframe in markdown format for all times.",
        "parameters": {
          "type": "OBJECT",
          "properties": {}
        }
      }
    ]
  }
]